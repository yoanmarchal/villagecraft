/** Nombre d'étages de la grille. */
export const GRID_HEIGHT = 10;

/** Bornes du côté de la grille (slider "gridSize", liens de partage). */
export const GRID_SIZE_MIN = 2;
export const GRID_SIZE_MAX = 5;
